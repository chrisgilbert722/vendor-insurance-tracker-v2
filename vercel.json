{
  "version": 2,
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs18.x"
    }
  },
  "builds": [
    { "src": "next.config.mjs", "use": "@vercel/next" },
    { "src": "src/app/api/**/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/src/app/api/$1" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
